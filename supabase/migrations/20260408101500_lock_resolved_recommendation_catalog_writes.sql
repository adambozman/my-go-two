DROP POLICY IF EXISTS "Authenticated users can insert resolved recommendation catalog"
ON public.resolved_recommendation_catalog;

DROP POLICY IF EXISTS "Authenticated users can update resolved recommendation catalog"
ON public.resolved_recommendation_catalog;
