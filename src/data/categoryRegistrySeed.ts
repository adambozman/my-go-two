/**
 * Seed data for the category registry.
 * Used by the admin CategorySync page to upsert categories into the database.
 */
export const CATEGORY_REGISTRY_SEED: Array<Record<string, unknown>> = [];
