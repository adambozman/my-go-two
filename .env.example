# Copy to .env and fill in. Do NOT commit .env (it is gitignored).
# The publishable/anon key is safe to ship in the browser bundle; the project URL/ref are not secret.
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
